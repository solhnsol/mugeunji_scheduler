# 이 파일을 .env 로 복사한 뒤 값을 설정하세요.
# docker compose up 전에 필수입니다.

# 관리자(admin) 최초 비밀번호 — DB에 admin 계정이 없을 때만 적용됩니다.
ADMIN_PASSWORD=change-me

# JWT 서명 키 — 프로덕션에서는 반드시 긴 랜덤 문자열로 변경하세요.
SECRET_KEY=change-me-to-a-long-random-string
